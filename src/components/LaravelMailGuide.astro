---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section class="py-20 bg-brand-bg relative overflow-hidden" id="laravel-mail-guide">
    <div class="container-custom">
        <div class="max-w-4xl mx-auto">
            <header class="text-center mb-12">
                <h2 class="text-3xl md:text-4xl font-black text-white mb-4">
                    What is Laravel Mail?
                </h2>
                <p class="text-xl text-gray-300 max-w-2xl mx-auto">
                    Laravel Mail is a powerful self-hosted email and marketing platform that is free.
                    Take full control of your email infrastructure without relying on third-party services.
                </p>
            </header>

            <div class="grid md:grid-cols-2 gap-8 mb-12">
                <article class="bg-brand-bg-card/40 backdrop-blur-md rounded-xl p-6 border border-white/10">
                    <h3 class="text-xl font-bold text-white mb-4 flex items-center gap-2">
                        <svg class="w-6 h-6 text-brand-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                        </svg>
                        Self-Hosted Infrastructure
                    </h3>
                    <p class="text-gray-300 mb-4">
                        Host on your own servers for complete data ownership and privacy. No monthly fees, no vendor lock-in.
                    </p>
                    <pre class="bg-gray-900 rounded-lg p-4 overflow-x-auto text-sm"><code class="text-green-400">git clone https://github.com/laravelmail/app.laravelmail.com.git
cd app.laravelmail.com
cp .env.example .env
php artisan key:generate
php artisan migrate</code></pre>
                </article>

                <article class="bg-brand-bg-card/40 backdrop-blur-md rounded-xl p-6 border border-white/10">
                    <h3 class="text-xl font-bold text-white mb-4 flex items-center gap-2">
                        <svg class="w-6 h-6 text-brand-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
                        </svg>
                        Marketing Automation
                    </h3>
                    <p class="text-gray-300 mb-4">
                        Build automated email sequences, drip campaigns, and trigger-based workflows.
                    </p>
                    <pre class="bg-gray-900 rounded-lg p-4 overflow-x-auto text-sm"><code class="text-green-400">// Create a campaign
$campaign = Campaign::create([
    'name' => 'Welcome Series',
    'subject' => 'Welcome aboard!',
]);

// Schedule emails
$campaign->scheduleEmail([
    'delay' => '1 day',
    'template' => 'welcome-intro',
]);</code></pre>
                </article>
            </div>

            <div class="bg-brand-bg-card/40 backdrop-blur-md rounded-xl p-8 border border-white/10 mb-12">
                <h3 class="text-xl font-bold text-white mb-4">Beautiful Email Templates</h3>
                <p class="text-gray-300 mb-4">
                    Design responsive HTML emails with the built-in editor or use Blade templates.
                    Preview and test before sending.
                </p>
                <pre class="bg-gray-900 rounded-lg p-4 overflow-x-auto text-sm mb-4"><code class="text-green-400">{`<!-- resources/views/emails/campaign.blade.php -->
<html>
    <body>
        <h1>{{ $campaign->subject }}</h1>
        <p>Hi {{ $subscriber->name }},</p>
        {!! $campaign->body !!}
        <a href="{{ $unsubscribeUrl }}">Unsubscribe</a>
    </body>
</html>`}</code></pre>
                <a 
                    href="/early-access" 
                    title="Learn how to deploy Laravel Mail for production"
                    class="inline-flex items-center text-brand-primary hover:underline"
                >
                    Deploy Laravel Mail for production →
                </a>
            </div>

            <div class="text-center">
                <h3 class="text-2xl font-bold text-white mb-4">
                    Laravel Mail Use Cases
                </h3>
                <div class="grid sm:grid-cols-2 md:grid-cols-4 gap-4 mt-6">
                    <div class="bg-brand-bg-card/40 backdrop-blur-md rounded-lg p-4 border border-white/10">
                        <span class="text-2xl mb-2 block">📧</span>
                        <h4 class="font-semibold text-white text-sm">Transactional Emails</h4>
                        <p class="text-gray-400 text-xs mt-1">Order confirmations, receipts</p>
                    </div>
                    <div class="bg-brand-bg-card/40 backdrop-blur-md rounded-lg p-4 border border-white/10">
                        <span class="text-2xl mb-2 block">📢</span>
                        <h4 class="font-semibold text-white text-sm">Marketing Campaigns</h4>
                        <p class="text-gray-400 text-xs mt-1">Newsletters, promotions</p>
                    </div>
                    <div class="bg-brand-bg-card/40 backdrop-blur-md rounded-lg p-4 border border-white/10">
                        <span class="text-2xl mb-2 block">🔔</span>
                        <h4 class="font-semibold text-white text-sm">Notifications</h4>
                        <p class="text-gray-400 text-xs mt-1">Alerts, reminders</p>
                    </div>
                    <div class="bg-brand-bg-card/40 backdrop-blur-md rounded-lg p-4 border border-white/10">
                        <span class="text-2xl mb-2 block">🤖</span>
                        <h4 class="font-semibold text-white text-sm">Automated Drips</h4>
                        <p class="text-gray-400 text-xs mt-1">Welcome series, follow-ups</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
